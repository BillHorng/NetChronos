# NetChronos 交接手冊

## 專案定位

NetChronos 是無須建置工具的純前端網路穩定度監控頁面。它會持續向使用者選擇的 HTTPS 信任端點發送探測請求，整理延遲、抖動、Loss、網路評級與診斷摘要。

目前 UI 內的產品名稱仍為 **NetWatch**；專案資料夾名稱為 **NetChronos**。若要正式發布，建議後續統一名稱。

## 執行方式

不需安裝套件。可直接開啟 `index.html`，但建議透過本機 HTTP 伺服器或正式 HTTPS 網站使用，以獲得較穩定的 Clipboard API 與跨網域請求行為。

1. 在右側「測試網站」下拉選單選擇端點。
2. 未選擇端點時，「開始監測」為灰階禁用狀態。
3. 選擇端點後開始監測；可設定探測間隔與逾時秒數。
4. 使用診斷摘要右上角的「複製摘要」提供資訊給 IT 或 ISP。

## 檔案與責任

| 檔案 | 責任 |
| --- | --- |
| `index.html` | 頁面語意結構、控制項、頂部即時資訊、圖表容器、事件與診斷摘要。另含少量覆寫式 CSS，用於單頁壓縮與最新布局。 |
| `styles.css` | 主要深色科幻控制台樣式、響應式布局、圖表／事件／面板樣式。 |
| `app.js` | 探測生命週期、白名單驗證、統計與評級、Canvas 圖表、摘要複製、用戶端資訊與互動事件。 |
| `HANDOFF.md` | 本文件。 |

## 目前版面

桌面版以 `100dvh` 顯示為單一控制台頁面，外層不需上下捲動：

1. 頂部整合面板：品牌、即時用戶端資訊、狀態、評級與四項指標。
2. 主工作區：左側延遲圖表，右側監測設定。
3. 底部雙欄：左側事件紀錄，右側診斷摘要。

高度不足時，右側設定面板、事件紀錄與摘要內容可在自身區塊內捲動。手機版（寬度 760px 以下）會改為垂直布局並允許頁面捲動。

## 功能細節

### 即時用戶端資訊

頂部顯示平台／瀏覽器、即時本地時間、時區、瀏覽器回報的連線類型與下行頻寬預估。這些資訊由 `updateClientMeta()` 更新；時間每秒更新一次。

不取得公開 IP 或地理位置，避免未經使用者操作就對第三方服務發出額外請求。

### 監測與 Loss

- 探測使用 `fetch()` 加上 `AbortController` 逾時控制。
- 成功樣本記錄回應耗時；失敗／逾時樣本列為 Loss。
- 樣本最多保留 240 筆，避免長時間運行造成記憶體持續成長。
- Loss 是 HTTP 探測失敗比率，不等同 ICMP 的封包遺失率。

### 圖表

Canvas 圖表支援最近 60／120 筆樣本，包含：

- 延遲折線與填滿區。
- 150 ms 警戒線。
- 平均延遲線。
- 高延遲點（橘色）與 Loss／逾時點（紅色）。
- 滑鼠移至資料點時顯示狀態、數值與時間。

### 評級規則

| 等級 | 條件 |
| --- | --- |
| A | Loss ≤ 1%、平均延遲 ≤ 90 ms、抖動 ≤ 30 ms。 |
| B | 超過任一 A 級門檻。 |
| C | Loss > 5% 或平均延遲 > 180 ms。 |
| D | 瀏覽器離線或連續 Loss 達 3 次。 |

至少取得 4 筆成功樣本後，才會顯示 A、B 或 C；否則維持等待樣本。

## 安全與隱私控制

UI 採下拉選單，程式會再次驗證端點；所有端點皆以圖片載入量測，不依賴 Fetch CORS：

- 僅允許 HTTPS。
- 僅允許白名單中的完整網址。
- 即使透過開發者工具修改選單值，也會拒絕非白名單端點。

目前白名單：

- 同網域 `probe.svg`（建議）：以圖片載入量測與網站同來源的回應時間，不受 Fetch API 的 CORS 限制。
- `https://www.google.com/favicon.ico`
- `https://www.cloudflare.com/favicon.ico`
- `https://www.microsoft.com/favicon.ico`

事件紀錄、摘要與圖表提示均以 `textContent` 或 DOM 節點寫入。專案不使用 `eval`、動態載入第三方 JavaScript、帳密儲存或背景資料上傳。

## 已知限制與後續建議

- 圖片端點若被公司網路、DNS、防火牆或瀏覽器擴充套件封鎖，會被判為 Loss；正式使用應優先選擇同網域 `probe.svg`。
- `navigator.connection` 並非所有瀏覽器支援；頻寬與連線類型可能不可用。
- 若需要真實全網頻寬、ICMP Loss 或設備監控，需加入後端服務，並串接路由器 API、SNMP 或監測代理程式。
- 正式環境建議設置自家 HTTPS 探測端點，減少對公開端點與第三方資料的依賴。
- 最新的版面覆寫 CSS 暫放於 `index.html` 的 `<style>` 中；後續重構時可移至 `styles.css`，讓樣式來源更集中。
- 正式部署時建議加入 CSP、HTTPS 安全標頭與部署層的存取記錄策略。
